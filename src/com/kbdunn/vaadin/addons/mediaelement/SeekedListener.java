package com.kbdunn.vaadin.addons.mediaelement;

public interface SeekedListener {
	// Fires when the user is finished moving/skipping to a new position in the audio/video
	void seeked(MediaComponent component); 
}
